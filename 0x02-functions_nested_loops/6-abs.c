#include <stdlib.h>
#include "main.h"

/**
* _abs - computes the absolute value of an integer.
* @i: int to be worked on
* Return: absolute value of an int
**/

int _abs(int i)
{
	int result;

	result = abs(i);
	return (result);
}